scalar Time

type Query {
    # TODO: Pagination
    events(limit: Int!, offset: Int!): [Event!]!
    event(id: ID!): Event!

    upcomingEvents(
        """
        If eventType is not provided, all upcoming events will be fetched.
        """
        eventType: EventType
    ): [Event!]!
}
